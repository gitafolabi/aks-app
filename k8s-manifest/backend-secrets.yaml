apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
type: Opaque
# Data is injected at runtime via CI/CD to avoid hardcoding credentials.
data: {}
# The actual secret data should be provided at deployment time from a secure source (e.g., Azure Key Vault).
# Do NOT hardcode sensitive values here.
