# Default values for Grafana
replicaCount: 1

image:
  repository: grafana/grafana
  pullPolicy: IfNotPresent
  tag: "10.0.0"

appName: grafana
namespace: monitoring

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  automount: true
  annotations: {}
  name: ""

podAnnotations: {}
podLabels: {}

podSecurityContext:
  fsGroup: 472

securityContext:
  runAsUser: 472
  runAsNonRoot: true

service:
  type: ClusterIP
  port: 3000

ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []

resources:
  requests:
    cpu: 100m
    memory: 64Mi
  limits:
    cpu: 300m
    memory: 512Mi

livenessProbe:
  httpGet:
    path: /api/health
    port: http
  initialDelaySeconds: 30
  periodSeconds: 10

readinessProbe:
  httpGet:
    path: /api/health
    port: http
  initialDelaySeconds: 5
  periodSeconds: 10

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80

# Grafana configuration
grafana:
  # To use External Secrets, set useExternalSecret to true
  # and ensure 'grafana-admin-password' exists in your Azure Key Vault
  useExternalSecret: true
  adminPassword: admin  # Fallback if useExternalSecret is false
  
  # Data sources
  datasources:
    prometheus:
      # Use the internal K8s service name. 
      # Format: http://<service-name>.<namespace>.svc.cluster.local:9090
      url: http://aks-prometheus-monitoring-monitoring-prometheus.monitoring.svc.cluster.local:9090
      
  # Dashboards  
  dashboards:
    enabled: true
